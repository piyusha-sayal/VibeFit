# backend
What type of app are you building: can you again stop the reading on png files, i don't wantto permanently enable png reads in claude settings
Language and tech stack: now tell me which free api key we need so we can do analysis
Who are the target users: So we can use both gemini and groq? so one can fallback on other if done. tell me where to add and model to choose
Data storage: let's do it, as long as everything is free?
Any constraints: why do we have a backend? how will this work in production?

# AI strategy
Use Haiku for simple tasks; Sonnet for main dev; Opus for complex arch or if Sonnet fails 2x.
Keep context low: Grep before Read, targeted reads only.

# Rules
Concise responses. No overengineering. No unrequested extras.